# ===========================================
# Book Character Analyzer Configuration
# ===========================================

# Which AI provider to use by default
# Options: openai, groq, sambanova, gemini, ollama
PROVIDER=openai

# ===========================================
# AI Provider API Keys
# ===========================================
# You only need keys for the providers you want to use
# Leave others empty if you don't plan to use them

# OpenAI (https://platform.openai.com/api-keys)
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# Groq (https://console.groq.com/keys) - Free tier available
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile

# SambaNova (https://cloud.sambanova.ai/) - Free tier available
SAMBANOVA_API_KEY=
SAMBANOVA_MODEL=Meta-Llama-3.1-8B-Instruct

# Google Gemini (https://aistudio.google.com/app/apikey)
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

# Ollama (local models, no API key needed)
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=llama3.2

# ===========================================
# Processing Settings
# ===========================================
# These control how the book is split and analyzed

# Size of each text chunk sent to AI (in characters)
# Smaller = faster but less context, Larger = slower but better accuracy
CHUNK_SIZE=2048

# How much chunks overlap (helps with continuity)
CHUNK_OVERLAP=200

# AI creativity level (0.0 = focused, 1.0 = creative)
# Keep low for consistent character extraction
TEMPERATURE=0.1